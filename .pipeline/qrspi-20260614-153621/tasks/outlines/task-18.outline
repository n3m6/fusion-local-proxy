Task: 18
Title: Documentation and example config
Phase: 5
Route: full
Slice: Observability, Tests, and Documentation
Dependencies: 17
Scope: Replace the one-line README.md with comprehensive documentation: architecture section (Mermaid diagram + port inventory table), local development setup (prerequisites, clone, install, env, dev server, verify, tests), usage examples (curl commands for OpenAI and Anthropic endpoints), and configuration reference (annotated fusion.config.json example with field reference table). Update fusion.config.json to provide a realistic example mixing at least one local provider (e.g., Ollama at http://localhost:11434/v1) and one remote provider (e.g., api.openai.com), with proper role assignments for panel, judge, and synthesizer.
Acceptance Criteria: AC-15
NFRs: NFR-1
Gate Criteria: Phase 5 Gate 2 (documentation complete)
Files:
  - README.md (MODIFY) — Replace one-liner with architecture section (Mermaid diagram + port inventory table), local development setup, usage examples, configuration reference, annotated fusion.config.json example
  - fusion.config.json (MODIFY) — Replace single-provider config with example mixing at least one local provider (baseURL: "http://localhost:11434/v1") and one remote provider (api.openai.com or api.anthropic.com); proper role assignments for panel, judge, synthesizer
